<%= simple_form_for @workout do |f| %>
  <%= f.input :title, label: "Workout name" %>
  <%= f.input :description %>
  <%= f.input :question_title, label: "Ask question" %>
  <%= f.input :question_description %>
  <%= f.button :submit %>
<% end %>
<%= link_to "Cancel", root_path %>
